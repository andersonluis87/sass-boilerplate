{
  "name": "server",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "build": "tsdown",
    "check-types": "tsc -b",
    "compile": "bun build --compile --minify --sourcemap --bytecode ./src/index.ts --outfile server",
    "dev": "tsx watch src/index.ts",
    "start": "node dist/index.mjs"
  },
  "dependencies": {
    "@fastify/cors": "^11.3.0",
    "@hono/trpc-server": "^0.4.2",
    "@sass-boiler-plate/api": "workspace:*",
    "@sass-boiler-plate/db": "workspace:*",
    "@sass-boiler-plate/env": "workspace:*",
    "@trpc/server": "catalog:",
    "dotenv": "catalog:",
    "fastify": "^5.10.0",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@sass-boiler-plate/config": "workspace:*",
    "@types/node": "catalog:",
    "tsdown": "^0.22.14",
    "tsx": "^4.23.1",
    "typescript": "catalog:"
  }
}
